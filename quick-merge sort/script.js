"use strict";

function quickSort(array) {
  // Implement the method known as quickSort to sort from smallest to largest
  // the array received as a parameter
  // Return the resulting ordered array
}

function mergeSort(array) {
  //Implement the method known as mergeSort to sort from smallest to largest
  //the array received as a parameter
  //Return the resulting ordered array
}
